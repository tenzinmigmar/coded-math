"""
Euclidean distance is distance
between two points in Euclidean
space.
"""
import math

def euclidean_distance(p, q):
    assert isinstance(p, tuple)
    assert isinstance(q, tuple)

euclidean_distance((3, 4), (7, 2))